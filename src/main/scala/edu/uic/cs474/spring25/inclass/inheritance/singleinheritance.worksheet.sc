/** In Scala, all classes (except for ...) inherit from one and only one
  * superclass. This means that there is one and only one class that is a
  * supertype of all classes (in type theory, we call this the top (⊤) type)
  *
  * Scala also has a bottom (⊥) type, which is ...
  */

classOf[Nothing]
